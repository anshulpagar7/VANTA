# VANTA
A benchmark for agentic revenue recovery. Four policies — naive retry, rule ladder, expected-value scoring, and the same scoring with LLM diagnosis — compete in a frozen simulated world with a held-out seed suite. The model cannot manufacture authority: only the policy engine can mint an executable action.
