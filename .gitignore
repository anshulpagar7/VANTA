__pycache__/
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/
*.sqlite3

# Secrets. Never commit. A key in git history survives its own deletion.
.env
.env.local
*.key

# Audit databases are regenerable and large; the HTML report is the artifact.
results/**/audit.sqlite3

# Holdout results stay unseeable until POLICIES_FROZEN exists.
results/holdout/
